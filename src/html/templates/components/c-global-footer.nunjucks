<footer class="c-global-footer">
  <div class="wrapper">
    <div class="copyright">
      <img src="{{globalFooter.copyright.imagePath}}" alt="globalFooter.copyright.imageDesc" />
      <a href="{{globalFooter.copyright.link}}">
        {{globalFooter.copyright.text}}
      </a>
    </div>
    <nav>
      <ul>
        {% for nav in globalFooter.navigation %}
          <li><a href="{{nav.url}}">{{nav.label}}</a></li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</footer>
