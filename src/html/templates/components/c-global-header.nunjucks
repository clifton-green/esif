<header class="c-global-header" role="banner">
  <div class="wrapper">
    <a href="{{header.logo.url}}" class="logo">
      <img src="{{ header.logo.imagePath }}" alt="{{header.logo.imageDesc}}">
      {{ header.logo.text }}
    </a>
    <div class="col">
      <h1>{{ header.propositionTitle }}</h1>
      {% include "components/c-global-nav.nunjucks" %}
    </div>
  </div>
</header>
