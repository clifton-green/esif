.c-application-info {
  border-top 5px solid $primary;
  padding: $vspacing 0;

  .title {
    font-size $smallCopy
    display block
  }
  .contents {
    font-weight bold
    display block
    margin-bottom $vspacing
  }
}
